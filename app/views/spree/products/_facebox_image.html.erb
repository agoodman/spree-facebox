<% if image -%>
<div class="product-image">
  <% if image_controls.enable_zoom? %>
    <%= link_to image_tag(image.attachment.url(:product), itemprop: "image"), rel: 'facebox' %>
  <% else %>
    <%= product_image(@product, itemprop: "image") %>
  <% end %>
</div>
<% end -%>
